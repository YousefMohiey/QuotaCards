{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions the app needs beyond its own commands. The speed page listens for speed-tick and speed-phase events from the backend, and the window moves from any empty surface, both of which are ACL-gated plugin commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
